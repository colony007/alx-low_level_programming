#include <stdio.h>
/**
 * main - Entry point of the program
 * description
 * Return: success (0)
 */
 int main(void)
 {
 	int ch;

	for (ch = 'z'; ch <= 'a'; ch--)
	putchar(ch);
	putchar('\n');
	return (0);
 }
