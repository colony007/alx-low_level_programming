#include <stdlib.h>
#include <stdio.h>
/**
 * main - entry point
 * description: prints all strings from 0 to 9
 * Return: success
 */
int main(void)

	for (int i = 0; i < 10; i++)
		printf("%d", i);

	printf("\n");

	return (0);
